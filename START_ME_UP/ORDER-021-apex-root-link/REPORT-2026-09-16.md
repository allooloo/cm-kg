# ORDER-021 — apex root link fix — Sept 16 2026 — CLOSED
Surface: https://mcp.capitalmarketsknowledgegraph.ai/ (Worker cm-kg-apex; hostnames mcp. + agent. bound at the account level, no zone routes).
Defect on the wire: the human root rendered every live-node link as https://.<node>-cm-kg.ai/mcp (label missing). The committed source (HEAD b28dcad and every prior commit) renders https://mcp.<node>.ai/ — the bundle live since the Sept 13 19:26 UTC deploy did not match the source in the repo. Cause of that mismatch not established; the template is now independent of it.
Fix: CM-KG/DOOR/apex/index.js human() — the Live-nodes links render from each node's `door` field in nodes.json (https://mcp.<cc>-cm-kg.ai), the field of record, not a rebuilt hostname. Version stays 0.12.1. Deployed with wrangler (version fdac622a…), zone purge_everything on capitalmarketsknowledgegraph.ai.
Verified after purge: 11/11 links → GET 200 at the node door root (ca uk au sg ch de fr nl jp kr us); /nodes.json, /facts.json, /llms.txt, /mcp.json, /openapi.json and the agent card byte-equal pre/post; serverInfo 0.12.1; resolve_issuer BARC / SHOP / 7203 / NESN byte-equal pre/post (the identifier index untouched: static/index.json and nodes.json unchanged, local == live core fields).
Contexts: az Allooloo (04a24e43… / 038b49c0…), gh allooloo — no switching.
Spend line: engines 0 calls / 0 tokens / $0 this order. Tavily (API): 93,727 of 100,000 plan credits used (Growth). Anthropic balance: not readable — no admin key on file under AGENT KEYS.
Evidence: pre/ post/ deploy.log.
