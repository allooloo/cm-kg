# ORDER-020 — CORPORATE + PRODUCT SURFACES, STATUS, RADAR, WARMING, FILINGS — closing report (Sept 12 2026)

Fly-by-wire; shape calls in `ORDER-020-BUILD-LOG.md` (42 items). Closed 2026-09-13 build-machine time after the Singapore line landed on the wire. Rule One held; the CEO's allooloo.io corrections were applied before anything else in the order and confirmed on the twelve node surfaces. Surfaces version 2026-09-12.5; every render since the first is in the pond (`POND\estate\surfaces\2026-09-12`, `-2`, `-3`).

## Record-grade standard applied — 21 surfaces + the record pages
One Worker (`estate-pages`), one chrome, one CSS file (`/estate.css`, 4 KB), one nav (nine tabs), one form, one footer — on allooloo.io, the twelve node surfaces and the eight product roots (21), plus /status, /record, /terms, /privacy, /security, /no-cookies and the estate roots. Every page: title line first, then the mark, nav, state pill with `as of` and version, mono H1, hairline-ruled numbered sections, tinted table headers; 4–30 KB; zero `<script>`; strict CSP, HSTS, nosniff, no-referrer, X-Frame-Options, Permissions-Policy, CMR headers; self-canonical, `<title>` + meta, WebSite JSON-LD "Allooloo", og:site_name, the favicon set (svg, ico, 48/96/144/192/512, apple-touch-icon); `/llms.txt`, `/facts.json`, `/.well-known/agent-card.json`, `/.well-known/security.txt`, `/sitemap.xml`, `/robots.txt` on every zone. Regions are never abbreviated: the full list with city and country appears in the node table, Proof, every node page's Residency clauses and section 7, Status, RADAR and the footer (short line, full list directly beneath). The kicker "Microsoft AI Cloud Partner · Microsoft Azure · eleven regions, in-country" opens the Proof section and the footer, never a page.

## allooloo.io — re-cut live (§4.0 order)
`AI Agents · MCP + A2A · Capital Markets Knowledge Graph — Allooloo` → mark → nav → LIVE pill → H1 "Every listed company in eleven markets, as a record an agent can call." → scale block (five live numbers from `list_nodes`: listed companies, dated disclosures, markets/regions served in-country, 100% fields with source URL and read date, last drop) → reason-for-being of record (Toronto and London first) → products (eight lines) → twelve-node table (live, regions in full) → four licence shapes → engines and duties → Proof (kicker, regions, sources, "what a record looks like" → `/record`, Status, RADAR, registry) → contact (both addresses, the README line, the form). The live hero record (a `get_record` for TSX:SHOP from the Canada door, sixteen fields with source, reader and state) is one link in Proof, on its own page at `/record`. Struck: "1T+ datapoints", "50,000+ organizations", "Three regions", "4,820 in one session", "Twelve nodes, each a public registry and a door". `/radar` → 308 → agentic-radar.ai. allooloo.io now renders from the estate Worker (custom domains re-bound with override; the old `allooloo-io` Worker stands, unrouted).

## Eight product pages — live with heads as given (§5, title rule §5.0)
| Root | Title of record | H1 (as given) |
|---|---|---|
| agentic-trades.ai | Agentic Trades for AI agents — the product line | The data systems agents trade on. |
| agentic-ask.ai | Agentic ASK for AI agents — ask about any listed company | One question, one door, twelve markets. |
| agentic-coverage.ai | Agentic Coverage for AI agents — a watchlist that reads the record | Every product on the shelf, watched weekly. (opens with Toronto, §12.3) |
| agentic-esg.ai | Agentic ESG for AI agents — sustainability disclosures as filed | What issuers disclosed on sustainability, as filed. |
| agentic-issuers.ai | Agentic Issuers for AI agents — your record, signed and live | The record every dealer agent reads about your company. |
| agentic-disclosure.ai | Agentic Disclosure for AI agents — the trail, dated and sourced | Every disclosure, dated, typed, sourced. Twelve markets. (opens with London, §12.3) |
| agentic-registries.ai | Agentic Registries for AI agents — who may call, and what they did | The registry of agents on the estate. ("gate not yet in service" stated) |
| agentic-radar.ai | Agentic RADAR for AI agents — the estate numbers | The estate, live, on one page. |

Reason-for-being paragraphs verbatim; bodies in the record-grade register with live numbers from the doors wherever a number appears. The twelve node surfaces carry the amended title grammar "<Country> Capital Markets Knowledge Graph for AI agents — <node-id>"; ca-cm-kg.ai and uk-cm-kg.ai carry the §12 buyer · duty · pain · first product · proof block above the fold.

## Nav and footer — count
Nine-tab nav and the one-row footer (Support · Status · Terms of Use · Privacy · Report a Security Issue · No cookies · llms.txt · Microsoft AI Cloud Partner · © 2026 Allooloo Technologies Corp.) with the kicker line and the full regions line above it: on all 21 surfaces, the six allooloo.io record pages and the six estate root/record hosts — 33 rendered pages, verified by fetch.

## Terms / Privacy / Security / No cookies — live
`/terms` and `/privacy` as record-grade numbered clauses, draft v0.1 as of 2026-09-12, `legal@allooloo.ai` to sign; `/security` (one page; `security.txt` on every zone); `/no-cookies` (one paragraph: no third-party scripts, no trackers, edge logs only).

## Status — live with the first 24h line
`allooloo.io/status` and `/status.json`: the estate Worker's cron sends one real `resolve_issuer` (each node's section-2 example) to each of the eleven doors every five minutes through its public hostname and writes the round trip to KV. First tick 21:25 UTC: eleven doors up (159–1,127 ms); the 24h line fills one sample per five minutes (288 a day). Hong Kong: beacon, partner wanted. One call per node per five minutes; no other polling anywhere.

## RADAR — live, fourteen widgets bound (§7)
agentic-radar.ai + `/radar.json` + `/radar.csv`, free, no registration. 01 The Record (list_nodes) · 02 The Doors (the probe) · 03 The Trail (list_nodes + Numbers Log) · 04 Calls (Cloudflare Workers analytics per day via `calls.json`; per-door and per-country counts need Zone Analytics Read — stated) · 05 Geography (same, stated not readable) · 06 Motion (Numbers Log lines) · 07 Provenance (100% by construction; README gaps per node) · 08 Residency (eleven regions in full; apex index only; no fallback) · 09 Registry (MCP registry read at render: v0.11.0 latest; twelve Agent Cards; filings) · 10 Machine surfaces · 11 Cite · Embed (citation, GET example, iframe, JSON/CSV) · 12 Spend (`spend.json`, the standing line) · 13 Broadcast · 14 Contact. No JavaScript; the LIVE clock is the render time.

## Warming — per region (§8)
Minimum 1 replica already set on every regional door (0.5 vCPU / 1 GiB, max 3, consumption plan): Canada Central, East US, UK South, France Central, West Europe, Switzerland North, Germany West Central, Australia East, Southeast Asia, Japan East, Korea Central — read and confirmed; the Status 24h line is the proof. Spend line below.

## Filings — state per registry (§9)
| Registry | State |
|---|---|
| Azure API Center (`allooloo-apic`, Free, `allooloo-cmkg-shared`) | apex + eleven doors registered as kind `mcp`, version v0.11.0, MCP definition, production deployment — 12 of 12 (REST API; the CLI extension has no MCP type). Sample `swagger-petstore` seeded by the service — standing, flagged. |
| MCP registry | v0.11.0 published, latest; bump on any door change (stage only, CEO go to publish). |
| Google enterprise MCP / agent catalogue | HITL — needs a Google Cloud console account; card of record ready. |
| Anthropic connector directory | HITL — submission form needs a human account; card of record ready. |
| GitHub MCP listing / A2A agent directories | HITL — as they stabilise; same card. |

## Sweeps — re-armed, first sweep lines (§10)
BUILD lock lifted (copy kept in `POND\.locks-lifted\`), uk and us node locks lifted the same way. Eleven Task Scheduler tasks "Allooloo CM-KG <cc> weekly" on each market's clock in build-machine time. Rail: harvest → immutable pond drop → regional store reload → apex rebuild → surfaces re-render → one Numbers Log line → snapshot. First sweep chain launched 15:21 build-machine time (nl → ch → fr → de → sg → au → uk → ca → jp → kr → us).

**First sweep lines — eight chain lines (Saturday 2026-09-12 sweep) plus the three standing 09-12 lines (Numbers Log, `NODE-SURFACES.md` §9):**

| Node | Line of record | Delta this sweep |
|---|---|---|
| nl-cm-kg | 2026-09-11 · 123 · 2,742 | events +35 |
| ch-cm-kg | 2026-09-11 · 829 · 1,512 | events +127 (corrected line; the first read hit a stale apex isolate) |
| fr-cm-kg | 2026-09-11 · 712 · 10,469 | events +470 |
| de-cm-kg | 2026-09-11 · 3,436 · 35,570 | events +167 |
| sg-cm-kg | 2026-09-12 · 639 · 3,620 | events +110 on the wire (the chain's own line read +0: its upload ran across the key rotation and the loader's per-run facts write — both fixed, items 35 and 37; corrected by the load-and-upload at chain end) |
| au-cm-kg | 2026-09-11 · 1,874 · 124,581 | events +281 |
| uk-cm-kg | 2026-09-11 · 1,570 · 128,191 | events −202 (consistent with the trailing twelve-month window rolling forward a day; not verified line by line) |
| ca-cm-kg | skipped by lock at the CEO's wrap (swept in the Saturday run) | 2026-09-10 · 4,820 · 25,222 stands |
| jp-cm-kg | standing 09-12 line | 2026-09-12 · 3,964 · 39,274 |
| kr-cm-kg | standing 09-12 line | 2026-09-12 · 2,802 · 143,297 |
| us-cm-kg | standing 09-12 line | 2026-09-12 · 7,710 · 1,872,343 |

**Record:** Saturday 2026-09-12 sweep complete, all eleven, one system. The 020 chain re-ran the same Saturday task a second time — a planning misstep in the new product, not an execution fault; the rail is proven and two defects were found and fixed on the way (the Numbers Log read after a deploy, item 17; the loader's per-run facts write, item 37). Every node lock lifted, copies kept; no lock stands.

**Schedule (SWEEP CLOCK, CEO Sept 12):** one task, "Allooloo CM-KG Saturday sweep" (`RAILS\sweep_all.ps1`, all eleven nodes, one run, in order) fires after 16:00 Eastern Friday when all eleven markets are closed — Friday 14:30 build-machine time (Central Standard Time (Mexico), UTC-6, no daylight shift) = 20:30 UTC = 16:30 Eastern. **Next fire confirmed by the scheduler: 2026-09-18 14:30 build-machine time (20:30 UTC), status Ready.** Each node's as-of is its sweep start in UTC; uploads follow overnight. The eleven per-node tasks are disabled and kept; nothing armed separately. Global BUILD lock lifted (copy kept). Next Saturday: the normal sweep, all eleven, one run.

**Queued for the pause:** the store reload should upload only the new drop, not the whole pond (the pond push is the slow step — about an hour on Singapore).


## Added during the order (CEO items after the boot, all live)
FORWARD allooloo.ai → 308 → allooloo.io (zone rule, records untouched) · footer single row, then X @allooloo_io and CEO Letter links · width 1200px on all surfaces, one column · nav all caps with a navy current-page underline · logo lockup of record · developers@allooloo.ai removed from every served surface (the form is the only support door) · favicon set of record · agent-readiness kit on all surfaces (allooloo.io and uk-cm-kg.ai Level 5 Agent-Native; remaining fails by cause: dnsAid DNS day, OAuth discovery and auth.md no authorization server by design, WebMCP zero scripts by design) · Contact Us form of record with the CSP form-action cause fixed and two real sends verified · identical top portion with a versioned stylesheet and a 60 s HTML cache · X-CMR-Contact header on the estate, apex and door image 0.11.2 · **kyp-model.ai as the 22nd surface** (seven zones audited, six forwards, page from the copy of record, clause 1.3 of record) · RADAR reason-for-being of record · SG storage key rotation applied. Surfaces version at close 2026-09-12.26; every render in the pond (`POND\estate\surfaces6-09-12` … `-17`).

## Deletions — the four approved items
The separate paste naming the four approved items had not arrived at close; **nothing deleted**. Standing candidates, each with its check ready to run before deletion: old `cm-kg-door` Worker; D1 `cm-kg`; `cmkg-env-eastus` (Failed); the failed US job executions; the 23 failed managed certificates; the old `allooloo-io` Worker (unrouted since this order). Named list awaited.

## HITL — needs MK
1. The four approved deletions — the paste, then each is checked and reported by name.
2. Google and Anthropic listings, GitHub/A2A directories — human accounts.
3. `legal@allooloo.ai` to sign Terms of Use and Privacy (draft v0.1 live).
4. Zone Analytics Read on the token of record — RADAR widgets 04/05 per door and per country.
5. Facts-vs-records gap on ch (829/626) and kr (2,802/2,759) — a re-render order.
6. Azure cost figure — Cost Management API 429 all day; spend line carries resources, not dollars.
7. Anthropic admin key; au storage key1 (standing).

## Spend line
- **Azure:** eleven doors at minimum 1 replica (0.5 vCPU / 1 GiB) in eleven regions; API Center Free tier added; no other resource added this order. Cost figure blank (API 429; consumption rows empty for the new groups).
- **Cloudflare:** KV namespace `cmkg-status` (free tier), cron on the estate Worker, two custom-domain re-binds; Worker deploys ×4.
- **Lab (this order):** none — no engine reads on 020; the sweeps' lab usage is written per node by their rails and reported per sweep from here (nl first sweep: wire search on the Tavily plan; counter read at close).
- **Standing lines** (spend.json, as reported): US rail Gemini ≈ USD 0.16; Japan Fill 4.50 / 16.57 / 18.79 / 2.53; Korea Fill 2.83 / 11.69 / 15.90 / 1.01 (USD, list); Tavily 80,283 of 100,000; Anthropic balance HITL.
