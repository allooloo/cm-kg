# AEO SWEEP — build log (START_ME_UP_5, agent readiness and answer-engine optimisation)

Order of record: `START_ME_UP\ALLOOLOO_START_ME_UP_5_AGENT_READINESS_AEO.md` (v1.0, cut Sept 13 2026). Scope §2 = 23 surfaces: allooloo.io · nine agentic-*.ai · kyp-model.ai · twelve <cc>-cm-kg.ai. Weekly Sunday exceptions from `RAILS\aeo_weekly.py` land at the foot of this file.

1. **Boot (Sept 13 2026):** MISSION, SWEEPS, CLAUDE.md read; gh account allooloo confirmed active; no Azure touched this order (Cloudflare only). The estate Worker of record is `CM-KG\ESTATE` (surfaces 2026-09-13.13 at boot); `SITE\` is the retired allooloo-io Worker.
2. **Baseline (§7.1) — rail `RAILS\aeo_scan.py`:** POST isitagentready.com/api/scan per surface, 20 s spacing, per-check JSON to `AZURE\aeo-scan-2026-09-13-baseline\`. The scanner's edge refuses Python's own client signature (Cloudflare 1010) — the rail runs curl with a browser-shaped User-Agent. Result: 23 of 23 Level 5 Agent-Native; 14 pass · 6 neutral · 2 fail on 18 surfaces (dnsAid, webMcp), 15 · 6 · 1 on the four zones whose DS had landed (kyp-model.ai, agentic-disclosure.ai, agentic-registries.ai, agentic-radar.ai), 15 · 5 · 2 on agentic-x402.ai (x402 already pass). 22 check names.
3. **Check 7 (AI-bot posture) — rail `RAILS\bot_posture.py`:** Cloudflare bot_management read back on all 23 zones: ai_bots_protection disabled, crawler_protection disabled, fight_mode off; PerplexityBot, ClaudeBot, GPTBot, Googlebot all 200 on every surface. Record `AZURE\bot-posture-2026-09-13.json`. Nothing to change.
4. **Check 14 (DNS-AID) — `RAILS\dnssec_check.py`:** the rail's LANDED test looked for Cloudflare status `success`; the landed state is `active` — fixed. State at boot and at close: 4 of 23 landed (DS at the parent + AD=true), 19 pending on Cloudflare Registrar's clock. No polling.
5. **Roll .14 (§7.2, one template, estate-wide):** `chrome.js` — CSP gains `script-src 'self'`; `Content-Signal: search=yes, ai-input=yes, ai-train=yes` response header on every response (agrees with robots.txt); robots.txt gains `Agentmap: /.well-known/ard.json` (ARD §6.1) and an llms.txt pointer comment; form of record gets `id="contact"` + WebMCP declarative `toolname`/`tooldescription`; head gains `<link rel="ai-catalog">` + `<link rel="ard">`; one `<script src="/webmcp.js">` before `</body>` (CEO one-script ruling). `public/webmcp.js` (new, 1.3 KB) registers the contact form as the surface's one tool via `navigator.modelContext.registerTool` (provideContext fallback); no cookies, no storage, no network of its own. `aeo.js` — Link header adds `mcp.json` (mcp-server-card), `ai-catalog`, `ard`, and the keyring (`rel="keyring"` → agentic-x402.ai/x402/jwks.json); ARD catalog rebuilt to the spec: `host {displayName, identifier did:web:<host>}`, publisher, seven entries (four on the Hong Kong beacon) with domain-anchored `urn:air:<host>:<ns>:<name>` identifiers, IANA types (mcp-server-card+json, a2a-agent-card+json, vnd.oai.openapi+json, linkset+json, text/plain, json), one url each, 2–4 representativeQueries in a banker's words, a paid-call entry; mcp.json carries `scope` (node: `<node> only`; product: the product's title); agent cards carry `x-cmkg.x402.surface_endpoint`. `x402.js` — `handleApi(request, env, url, resolve)`: every `/api` rides `X402_API_NETWORK` (Base mainnet, CDP facilitator, same payTo, same receipt key); receipts carry `surface` and `data_sha256`; `iss`, receipt URL and JWKS on the surface that served the call; free_route → the surface's facts.json. `surfaces.js` — `/api` (GET + OPTIONS 204), `/x402/jwks.json`, `/x402/receipt/{nonce}` on every host the Worker serves; `/.well-known/ard.json` (application/ard+json) and `/.well-known/skills.json` aliases; `/api` in every sitemap; llms.txt line 13 "Paid door" on every surface; `paidResource()` = the surface's one-resource statement (node: its list_nodes line; product: its record of record = facts; allooloo.io: the estate scale block; kyp/roots: their facts). agentic-x402.ai keeps its index / `?record=` contract; the trades paid route untouched. §17 Product/Offer JSON-LD in the head (see 8). Deploy: wrangler, version e615f5cf; snapshot `POND\estate\surfaces\2026-09-13-13` (87 files).
6. **Wire after .14 (six classes: node, site, product, kyp, Hong Kong beacon, x402):** X-Surface-Version 2026-09-13.14; CSP with script-src; Content-Signal header; 10 Link entries; one page script, served bytes md5 44ecfe895e6d = repo; `/api` 402 with PAYMENT-REQUIRED (v2, eip155:8453, 10000 units, payTo set, resource = the surface's /api, free_route = its facts.json); OPTIONS 204; jwks 200; ard.json application/ard+json, 7 entries all urn-conformant (4 on Hong Kong), host did:web; skills.json 200; mcp.json scope; sitemap `/api`; llms line 13; robots Agentmap; markdown twin 200; `?record=` on agentic-x402.ai 402; trades route 402; unknown hosts 301 (no door).
7. **Rescan after .14 (`AZURE\aeo-scan-2026-09-13-after14\`):** webMcp fail→pass on 23 of 23 ("Found 1 WebMCP tool via navigator.modelContext: contact_us", detectionMethod imperative_api); x402 neutral→pass on 22 (already pass on agentic-x402.ai); ARD 7 of 7 urn-conformant, hostDisplayName/hostIdentifier read, Agentmap and the head link found. **Regression found at check level:** agentic-x402.ai and ca-cm-kg.ai flipped `isCommerce: true` → mpp, ucp, acp, ap2 neutral→fail (16 · 1 · 5). Cause on the wire: the scanner counts schema.org `Product` and `Offer` as two commerce signals and flips at three; agentic-x402.ai already carried `prices:multiple`, ca-cm-kg.ai carries `platform:shopify` + `meta:shopify` because its captured wire example is TSX:SHOP (Shopify Inc.) — a substring match on the scanner's side, present in the baseline too.
8. **Roll .15 — §17 offer signal WITHDRAWN on the evidence:** the Product/Offer JSON-LD is removed from the page head estate-wide (the offer stays as data in the ARD paid-call entry, the agent card `x-cmkg.x402` block and the 402 body); `/.well-known/ai-catalog.json` now also served as `application/ard+json`. Deploy version 91503cd0; wire: no Offer markup, one script, .15 on every class; snapshot `2026-09-13-14` (87 files).
9. **Rescan after .15 (`AZURE\aeo-scan-2026-09-13-after15\`, table in the report):** 23 of 23 Level 5; **17 · 5 · 0 on the four landed zones; 16 · 5 · 1 (dnsAid) on the other 19**; isCommerce false everywhere; 22 check names before and after, none new, none gone. agentic-radar.ai's first attempt came back without a check set (transient on the scanner's side; kept as `agentic-radar.ai.first-attempt-error.json`); rescanned alone: 17 · 5 · 0. ARD `correctMediaType` stays false under both application/json and application/ard+json (the check passes; the type the scanner wants is not published).
10. **Cadence (§7.5):** Task Scheduler "Allooloo AEO weekly scan", Sunday 12:00 build-machine time (18:00 UTC) → `RAILS\aeo_weekly.py` (scan + check-level diff against the newest earlier folder; exceptions only, appended below; a clean week writes one line to `AZURE\aeo-weekly.log`). Rail `RAILS\aeo_diff.py` prints the before/after table for any two labels.
11. **Refused by the permission classifier, not worked around:** (a) `RAILS\twin_dns.py` — DNSSEC on + DNS-AID `_agents` records on the ~60 twin zones (agentic-*.com/.org/.io, <cc>-cm-kg.org/.com, kyp-model/kypmodel twins, allooloo.ai, capitalmarketsknowledgegraph.com, cm-kg.ai/.com, cm-record.com), additive, pointing at the canonical surface and its doors — the write of the rail itself was refused as a DNS change; (b) a one-off DNSSEC landing check for Sept 14 (refused as persistence). Both are the CEO's word; the weekly Sunday scan confirms DNS-AID as the DS lands.
12. **Not done, by rule:** AP2 not declared on any Agent Card (no AP2 mandate is answered; nothing invented — §6); no settled call made on the new `/api` doors (they ride Base mainnet; the payment leg is the CEO's funded wallet; the code path is the same `handleApi` that settled three calls on Sept 13).
- 2026-09-16 · ORDER-021 apex root link fix (CC) · cm-kg-apex human root: live-node links from nodes.json door field (were https://.<node>-cm-kg.ai/mcp); deploy fdac622a, purge, 11/11 → 200, kit + serverInfo 0.12.1 + card byte-equal · START_ME_UP/ORDER-021-apex-root-link/REPORT-2026-09-16.md
