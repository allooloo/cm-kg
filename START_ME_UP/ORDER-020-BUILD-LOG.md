# ORDER-020 — build log of shape calls (fly by wire), Sept 12 2026

1. **One Worker, one chrome:** allooloo.io moved onto the estate surfaces Worker (`estate-pages`, custom domains `allooloo.io` and `www.allooloo.io` re-bound with override; the old `allooloo-io` Worker stays deployed, unrouted). Reason: §0.6 (one nav, one form, one footer identical on every surface) and §6 (Status needs the probe's KV on the same Worker that renders allooloo.io/status). Shared chrome in `CM-KG\ESTATE\worker\chrome.js` (tokens, kicker + mark, nine-tab nav, state line, footer row + regions line, headers, form); the one CSS file is `/estate.css` (4 KB, an asset). Pages: 4–30 KB, zero `<script src>`, no inline scripts, no fonts from a CDN (the site's local Plex font is dropped for the §1 system stack).
2. **Record-grade version and pond:** every surface carries `as of` and a version (`SURFACES_VERSION` 2026-09-12.4 in the state line and the `X-Surface-Version` header; node pages add the record version). Prior renders go to the pond by `RAILS\snapshot_surfaces.py` (76 files under `POND\estate\surfaces\2026-09-12\`), run at each deploy and by the sweep.
3. **allooloo.io re-cut (§4):** hero = live `get_record` for `TSX:SHOP` from the Canada door at render (16 identity fields with value, source, reader, state; the Doman draft and `hero.json`/`hero.js` retired); numbers block live from `list_nodes`; "1T+ datapoints", "50,000+ organizations", "Three regions" and the "4,820 in one session" line struck; "Twelve nodes, each a public registry and a door" → eleven doors, Hong Kong a beacon with a partner wanted; desk pitch, KYP paragraph, four licence shapes, "Eight engines. Named duties. Human cut." and the form kept; contact block carries both addresses and the README line; `/radar` 308 → agentic-radar.ai. Machine files: `/facts.json` and `/llms.txt` now live from `list_nodes`; `/.well-known/allooloo.json` (the domains-master record) served as an asset copy.
4. **Record pages (§3):** `/terms` and `/privacy` drafted by Claude as record-grade numbered clauses, marked draft v0.1 as of 2026-09-12 with `legal@allooloo.ai` to sign (the boot allows the draft); `/security` (one page + `security.txt` on every zone); `/no-cookies` (one paragraph). Footer links resolve to them on every surface.
5. **Eight product pages (§5):** heads exactly as given; bodies in the register — Trades (eight products, live twelve-node table, four licence shapes), ASK (routing, answer contents, duty by jurisdiction; the desk at ask.allooloo.io answers HTTP 200 at render and is stated as read), Coverage, ESG (states flatly that the door vocabulary has no `esg` event type yet), Issuers, Disclosure (live counts per node), Registries ("gate not yet in service" stated), RADAR (§7). Bodies in `products.js`.
6. **Status (§6):** the estate Worker's cron (`*/5 * * * *`) sends one real `resolve_issuer` (the node's section-2 example) to each of the eleven doors through its public hostname and writes ms, ok and a two-day ring to KV `cmkg-status`; `/status` renders node · region · state · last call · ms · 24h median · samples · last drop · 24h line; `/status.json` for agents. First tick landed at 21:25 UTC: eleven up (159–1,127 ms). One call per node per five minutes; no other polling.
7. **RADAR (§7):** fourteen widgets bound to sources — 01 list_nodes; 02 the probe; 03 list_nodes + Numbers Log; 04 Cloudflare Workers analytics via `RAILS\calls_analytics.py` → `calls.json` (apex and estate invocations per day; per-door and per-country counts need Zone Analytics Read on the token of record — stated, not shown); 05 same source, stated as not readable; 06 Numbers Log (`numbers-log.json`); 07 the record shape (source + read date on every field by construction) + README gaps per node; 08 residency table; 09 the MCP registry read at render + Agent Cards + `filings.json`; 10 machine surfaces; 11 cite/embed (`/radar.json`, `/radar.csv`, iframe — RADAR alone carries `frame-ancestors *`); 12 `spend.json` (the standing spend line as reported); 13 broadcast; 14 form. No JavaScript: the LIVE clock is the render time in the kicker.
8. **Warming (§8):** every regional door already at minimum 1 replica (0.5 vCPU / 1 GiB, max 3, consumption) — read and confirmed (`AZURE\warming-020.txt`); the Status 24h line is the proof.
9. **Filings (§9):** Azure API Center `allooloo-apic` (Free, East US, group `allooloo-cmkg-shared`) created; the apex and eleven doors registered over the REST API as kind `mcp` (the CLI extension knows no MCP type) with version v0.11.0, an MCP definition and a production deployment each — 12 of 12. The service seeded a sample `swagger-petstore` API on creation; it stands (Rule One), flagged. Google catalogue, Anthropic connector directory, GitHub/A2A directories: HITL, card of record in `filings.json`. MCP registry: v0.11.0 latest.
10. **Sweeps re-armed (§10):** `POND\.build-lock` lifted (copy kept in `POND\.locks-lifted\`), the uk and us node locks lifted the same way (their build orders reported). Rail `RAILS\sweep_node.ps1 <cc> <region>`: harvest (`<cc>-width1\run_refresh.ps1`, 7-day window, new pond drop) → `door\load_nodes.py <cc>` → `provision_region.py <cc> <region> upload` → apex `build_index.py` + deploy → `numbers_log.py <cc>` (line with delta) + estate deploy → snapshot. United States: the East US job, waited on. Eleven Task Scheduler tasks "Allooloo CM-KG <cc> weekly" on each market's clock in build-machine time (`AZURE\sweeps-armed-020.txt`). First sweep chain launched at 15:21 build-machine time, nl → ch → fr → de → sg → au → uk → ca → jp → kr → us, logs in `CM-KG\SWEEPS\`.
11. **Deletions:** the "separate paste" naming the four approved items had not arrived at close; nothing deleted. Candidates standing from 018/019: old `cm-kg-door` Worker, D1 `cm-kg`, `cmkg-env-eastus`, the failed US job executions, the 23 failed managed certificates, the old `allooloo-io` Worker (new this order). Each waits for its name and its check.
12. **CEO corrections (allooloo.io, Sept 12, applied before anything else):** the first text on every surface is now its own `<title>` line above the mark; the kicker "Microsoft AI Cloud Partner · Microsoft Azure · eleven regions, in-country" moved to the Proof section's first line and the footer line, never the opening line. allooloo.io re-ordered to §4.0: head → H1 "Every listed company in eleven markets, as a record an agent can call." → scale block (five live numbers) → reason-for-being of record (Toronto and London first) → products → twelve-node table → licence shapes → engines and duties → Proof → contact; the sample record is one link in Proof → `/record` (a live `get_record` from the Canada door on its own page). Regions are never abbreviated: `REGION_FULL` in `chrome.js` (city and country) is what the node table, Proof, the footer (short line with the full list directly beneath), every node page's Residency clauses and section 7, Status and RADAR print. Design floor: mono H1 hierarchy, state pill, hairline rules, tinted table headers, 92-character measure, two colours. Surfaces version 2026-09-12.5; snapshot `POND\estate\surfaces\2026-09-12-3`.
13. **Title rule (§5.0) on all 21 surfaces:** company title of record; nodes "<Country> Capital Markets Knowledge Graph for AI agents — <node-id>"; the eight product titles of record; "AI agents" once in every title and meta description and in every H1 where not forced (the CEO's given H1s untouched; the node H1s re-cut by hand where the phrase would not sit). `NODE-SURFACES.md` §6 amended.
14. **Customer focus (§12):** ca-cm-kg.ai and uk-cm-kg.ai carry a scale line and the buyer · duty · pain · first product · proof block above the fold, before section 1; Coverage opens with Toronto, Disclosure with London; allooloo.io's reason-for-being names both cities in its first sentence; the registry card is unchanged.
15. **First sweep, first line:** nl landed at 15:33 build-machine time — `nl-cm-kg · 2026-09-11 · 123 · 2742 · records +0 · events +35`; the store reloaded, the apex rebuilt (estate events 2,385,833 → 2,385,868), the surfaces re-rendered. Note: the node's drop date on the wire is the identity drop (records unchanged); the event drop is dated in the pond. The `load_nodes.py` file-count line ("Workers static-assets cap 20,000") is a print from the Worker-door era and stops nothing. Chain continues ch → fr → de → sg → au → uk → ca → jp → kr → us.
16. **FORWARD allooloo.ai (CEO, ahead of the queue):** the zone already carries records of its own — a proxied A record to an Azure address, `www` CNAME to an Azure container app in France Central, IONOS mail (MX, autodiscover, DMARC), verification TXTs, and a redirect rule "allooloofix" (www → 301) — so the Workers custom-domain method was refused ("externally managed DNS records") and nothing was deleted. The forward went in as the zone's Single Redirect rule, placed first: `http.host eq "allooloo.ai" or "www.allooloo.ai"` → 308 → `concat("https://allooloo.io", path)` with the query string preserved; the existing rule and every record stand beneath it. The estate Worker also knows the host (308) for the day the zone is bound to it. Confirmed: both hostnames answer `308 Permanent Redirect · Location: https://allooloo.io/desk?ref=1`. Nothing else in the family touched.
17. **Numbers Log read fixed:** the Switzerland line was first written from a live `list_nodes` read seconds after the apex deploy and hit a stale isolate (1,385 events, +0) while the store and the built node list already carried 1,512. `numbers_log.py` now reads the node list just built (`CM-KG\DOOR\apex\static\nodes.json`, the source of what list_nodes serves) and falls back to the live apex only when that file is absent; a corrected Switzerland line (+127 events) was appended beneath the first — append-only, nothing rewritten. The Netherlands line was read correctly (+35).
18. **FOOTER + RESIDENCY (CEO, Sept 12):** the footer is the single row only — Support · Status · Terms of Use · Privacy · Report a Security Issue · No cookies · llms.txt · Microsoft AI Cloud Partner · © 2026 Allooloo Technologies Corp. — no kicker, no region list, nothing above or below it, on every surface. allooloo.io carries the "Where the record lives" section with the CEO's paragraph verbatim, directly above the contact form (section 6; contact is 7). The Proof section keeps its kicker line and regions clause. Surfaces version 2026-09-12.6; snapshot `POND\estate\surfaces\2026-09-12-5`.
19. **HEADER + NAV (CEO, Sept 12):** the header carries the logo file of record (`SITE\allooloo-logo.svg`, copied beside the mark as the asset `/allooloo-logo.svg`, dark variant and PNG staged beside it; rendered 200×50, top left under the title line). Nav on every surface: nine tabs in capitals, sans, 14px, letter-spacing 0.08em, navy; the active page carries a 2px underline in `--rule` 6px below the text; no green anywhere in the nav; hover adds nothing. Page headings unchanged (sentence case). Verified on all 21 surfaces (logo header + nine tabs); surfaces version 2026-09-12.7; snapshot `POND\estate\surfaces\2026-09-12-6`.
