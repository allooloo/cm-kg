# X402 TEST RECEIVER + TO 100 — build log (CEO orders, Sept 13 2026)

Shape calls logged, not asked. Nothing deleted or overwritten; new output beside old.

## X402 TEST RECEIVER (agentic-trades.ai)

1. **Route:** `worker/x402.js` — `GET /x402/record/{node}/{exchange}/{code}` on agentic-trades.ai only; the same record as the free apex route; requirements `{scheme exact, network eip155:84532 (Base Sepolia first), amount 10000 (= $0.01 USDC, 6 decimals), asset USDC 0x036C…dCF7e, payTo env.X402_PAYTO}`; 402 with JSON body + `PAYMENT-REQUIRED` header; client pays with `PAYMENT-SIGNATURE` / `X-PAYMENT`; facilitator `https://x402.org/facilitator` `/verify` then `/settle`; on settle → 200 record + `PAYMENT-RESPONSE`; KV `x402:count`, `x402:last` (tx hash, explorer link). Mainnet (`eip155:8453`, CDP facilitator, `CDP_API_KEY`) switchable by secret `X402_NETWORK=base` on the CEO's word. Free routes untouched (verified 200).
2. **RADAR:** widget 12 carries the "Paid calls (x402)" line; `radar.json` carries `paid_calls`.
3. **Receiver state:** the route answers `503 receiver_not_configured` until the pay-to address is set. The address is given out of band and is not on disk: drop it at `AGENT KEYS\x402-payto.txt` (address only, one line); CC then runs `wrangler secret put X402_PAYTO < file` (never printed) and the route answers 402. The payment leg (402 → pay → 200) is run from the CEO's funded wallet: CC does not transfer funds and does not hold wallet keys. Report line (route live · tx hash · amount) closes when that call lands.
4. Surfaces 2026-09-13.2; commit `ca23260`.

## TO 100 — item 1, REGISTRIES GATE (agentic-registries.ai in service)

5. **Key of record:** ES256 P-256 pair generated locally; private JWK `AGENT KEYS\oauth-jwk.txt` (git-ignored, never printed), Worker secret `OAUTH_JWK`; public JWK `public/oauth-jwks.json` and `https://agentic-registries.ai/oauth/jwks.json`, kid `allooloo-registry-2026-09-13`.
6. **Authorization server** `worker/oauth.js`: issuer `https://agentic-registries.ai`, audience the apex door; RFC 8414 metadata at `/.well-known/oauth-authorization-server` and `/.well-known/openid-configuration` on every one of the 22 surfaces; RFC 7591 `POST /oauth/register` (KV `oauth:client:<id>`, secret hashed, client_secret shown once, `registration_client_uri` gated to the owner); `POST /oauth/token` client_credentials (form or HTTP Basic) → ES256 JWT 3600 s; `/oauth/introspect`; token receipts `oauth:receipt:<jti>` (30 d).
7. **Licensed routes gated by `verify()`** (401 + `WWW-Authenticate: Bearer realm, resource_metadata, error` otherwise): `agentic-trades.ai/licensed/record/{node}/{exchange}/{code}` (byte-identical to the apex record, `X-Licensed-Client` header) and `agentic-registries.ai/registry/whoami`. Public routes untouched.
8. **Protected-resource metadata** on every surface now names `authorization_servers: [https://agentic-registries.ai]`, `bearer_methods_supported: [header]`, the three scopes.
9. **Registries page copy** (products.js): "gate not yet in service" → "gate in service (Sept 13 2026)" with the spec lines (metadata, register, token, jwks, introspect, licensed routes, PRM scopes); on-ramp and revocation clauses re-cut to the live flow.
10. **Wire test (surfaces .3):** metadata 200 on allooloo.io, uk-cm-kg.ai, agentic-registries.ai, agentic-trades.ai, kyp-model.ai; register 201; token 200 (bad secret 401); licensed record 200 with token, 401 `missing_token` without; whoami 200; introspect active true / tampered false; free routes 200. Commit `ca23260`.
11. **Rescan after .3:** allooloo.io, uk-cm-kg.ai, kyp-model.ai — 13 pass / 6 neutral / 3 fail; `oauthDiscovery` PASS (was fail), `oauthProtectedResource` PASS; `authMd` still fail: "Authorization Server metadata has no agent_auth block" — the scanner reads auth.md as the WorkOS auth.md protocol (agent_auth block in the AS metadata: identity/claim/events endpoints, identity types).
12. **auth.md conformance (surfaces .4):** `agent_auth` block in the AS metadata (skill = auth.md, identity/claim/events endpoints, `identity_types_supported: [anonymous]` — ID-JAG and service_auth stated as not offered); `POST /agent/identity` (anonymous → registration + identity_assertion JWT typ `identity_assertion` 30 d + claim_token + client credentials); `POST /agent/identity/claim` (attaches a contact; registrations are active on issue, no sign-in ceremony); `POST /agent/event/notify` (202, RFC 8935 events logged to KV); `POST /oauth/revoke` (RFC 7009, KV `oauth:revoked:<jti>`, checked by verify); `grant_types_supported` + `urn:ietf:params:oauth:grant-type:jwt-bearer` (assertion → access token). `auth.md` rewritten to the protocol's sections (Discover · Pick a method · Register · Claim ceremony · Exchange the assertion · Use the access_token · Errors · Revocation) with http/json blocks. Shape call: anonymous only — Allooloo has no user accounts to bind, so ID-JAG and email claim ceremonies would be theatre.
13. **Flow test (.4):** identity 201 → claim 200 complete → jwt-bearer token 200 → licensed record 200 → assertion used as an access token 401 `wrong_token_type` → revoke 200 → whoami 401 `revoked` → events 202 → service_auth 400 `unsupported_identity_type`. Commit `9bca722`.

## TO 100 — item 2, DNSSEC (DNS day)

14. **Survey:** all 22 served zones at Cloudflare Registrar (RDAP "Cloudflare, Inc"; the master workbook agrees — the only IONOS rows are allooloo.org/.com, forwards, not served). DNSSEC was disabled on all 22, no DS anywhere. `AZURE\dnssec-survey-2026-09-13.json`, `rdap-registrars-2026-09-13.json`.
15. **Enable:** `PATCH /zones/{id}/dnssec {status: active}` on the 22 zones (token cloudflare.txt, DNSSEC Edit scope) → all `pending`, DS present (key tag 2371, algorithm 13, digest 2), Cloudflare Registrar publishes the DS to the .ai / .io registries itself (no HITL). DS list of record: `AZURE\dnssec-ds-2026-09-13.json`.
16. **DNS-AID records** (draft-mozleywilliams-dnsop-dnsaid; the scanner's dnsAid check queries `_index._agents` SVCB/HTTPS/TXT, `_mcp._agents`, `_a2a._agents`): `RAILS\dns_aid_records.py` created 86 records, none pre-existing — per zone `_index._agents` SVCB 1 → the surface (alpn h2, port 443) + TXT `agents=records:mcp,estate:a2a`; `_mcp._agents` SVCB → the node's MCP door (`mcp.<cc>-cm-kg.ai`) or the apex; `_a2a._agents` SVCB → the agent-card host; Hong Kong (beacon) index only. Resolve verified over DoH on allooloo.io, uk-cm-kg.ai, hk-cm-kg.ai. `AZURE\dns-aid-records-2026-09-13.json`.
17. **Scheduled later check, no polling:** `RAILS\dnssec_check.py` (Cloudflare status + DS at the parent + AD flag on the `_index._agents` TXT) as Task Scheduler "Allooloo DNSSEC landing check 2026-09-13", one-off 12:30 build-machine time = 18:30 UTC → `AZURE\dnssec-check-<stamp>.txt/json` (+ `dnssec-check-scheduled.log`). Baseline at 14:10 UTC: 0 of 22 landed (all pending, DS not yet at the parent). dnsAid passes when DS lands and the validating resolver sets AD.
18. **Rescan after .4 (records + auth.md):** allooloo.io, uk-cm-kg.ai, kyp-model.ai — each Level 5, 14 pass / 6 neutral / 2 fail. `oauthDiscovery` PASS, `authMd` PASS ("Auth.md support detected (anonymous)"), `oauthProtectedResource` PASS. Remaining fails: `dnsAid` ("records found, but DNSSEC was not validated" — waits on the DS landing) and `webMcp` (needs a page script; zero-script rule stands). `AZURE\agent-readiness-scan-2026-09-13-authmd.json`. Report: `X402-TO100-REPORT-2026-09-13.md`.
19. **x402 → Base mainnet (CEO: "pay-to on disk, Base.", ~14:50 UTC):** pay-to read from `AGENT KEYS\x402-payto.txt` (EVM address, one line) into Worker secret `X402_PAYTO`, never printed; `X402_NETWORK=base`. Route answers **402** with `{scheme exact, network eip155:8453, amount 10000, asset USDC 0x8335…2913, payTo <CEO address>}` + `PAYMENT-REQUIRED` header; RADAR `paid_calls` shows receiver configured, network eip155:8453. Facilitator shape call: the x402.org hosted facilitator serves Base Sepolia only (checked `/supported`: no eip155:8453); Coinbase CDP on mainnet needs a CDP Secret API key which is not on disk (401 without it). So the Worker now selects the facilitator per network: CDP with a per-request JWT (Ed25519 or ES256, code in `x402.js` `cdpJwt`) the moment `CDP_API_KEY_ID` + `CDP_API_KEY_SECRET` are set, and until then the keyless PayAI facilitator `https://facilitator.payai.network` (x402 v2 `exact` on eip155:8453, checked `/supported`), `X402_FACILITATOR` overrides. Facilitator wire verified (a dummy verify answers `invalid_payload`; no funds moved). Surfaces 2026-09-13.5. The 402 → pay → 200 call is the CEO's funded wallet; the settled tx hash and amount land in KV `x402:last` and on RADAR.

## AGENTIC-X402.AI — 23rd surface (CEO order, Sept 13 2026, ~15:20 UTC)

20. **Zones:** agentic-x402.ai/.com/.org/.io were in the account (active, no records). Bound all four to the estate Worker as custom domains (`PUT /accounts/…/workers/domains`, service estate-pages), SSL full (strict). AI-bot block: read via `/bot_management` — `ai_bots_protection: disabled`, `fight_mode: false`, `crawler_protection: disabled` on all four (already OFF; nothing to change, the token has no Bot Management scope anyway). .com/.org/.io → 301 → agentic-x402.ai, path preserved (the house forward; verified after propagation). DNSSEC enabled on agentic-x402.ai (pending, DS held) and DNS-AID `_agents` records created (4); both rails now list 23 zones.
21. **Page** (`products.js` `PRODUCTS.x402`, shared template): title of record "Agentic x402 for AI agents — a paid call with a signed receipt", H1 "One call, one cent, one receipt."; sections What x402 is · What this endpoint does · The exchange (402 → signed authorization → 200 + receipt) · The 402 challenge as protocol fact · Receipts · Machine surfaces · Identity headers · Access · Provenance · Estate · Machine kit · Contact Us. The pay-to address is not rendered (checked: no 40-hex string on the page); it is stated only in the 402 body, as the protocol requires. Bug found and fixed: the product-host regex took `[a-z]+` and refused the digits in "x402" (404 on first fetch) → `[a-z0-9]+`.
22. **Endpoint `/api`** (`x402.js` `handleApi`): $0.01 USDC (10000 units), scheme exact = EIP-3009 transferWithAuthorization, `maxTimeoutSeconds: 60`, payTo from secret `X402_PAYTO` (the CEO's file). Network per route: `/api` rides `X402_API_NETWORK` (default base-sepolia = **testnet first**, facilitator x402.org); the trades record route stays on Base mainnet per the earlier word. What the call buys: the estate index (`nodes.json`) or, with `?record=node/exchange/code`, one Capital Markets Record — the same data the free routes serve. Verified: 402 with `PAYMENT-REQUIRED` header, network eip155:84532, USDC 0x036C…dCF7e, window 60, resource stated; `?record=` variant 402 too.
23. **Receipts:** Ed25519 pair generated (`AGENT KEYS\x402-receipts-jwk.txt` private, git-ignored; Worker secret `X402_RECEIPT_JWK`; public `public/x402-jwks.json` and live `/x402/jwks.json`, kid `allooloo-x402-receipts-2026-09`, crv Ed25519, no `d`). On every settlement: JWT (EdDSA) with iss agentic-x402.ai, sub payer, jti nonce, iat, resource, network, asset, amount, payTo, transaction + explorer, settled, facilitator, receipt_url; stored in KV `x402:receipt:<nonce>` with no expiry (forever); resolver `/x402/receipt/{nonce}` answers immutable (cache one year), 404 for unknown nonces, 404 for malformed. Answer body `{ receipt, data }`; headers `PAYMENT-RESPONSE`, `X-PAYMENT-RESPONSE`, `X-X402-Receipt`. Every settlement increments the RADAR paid-calls line (`x402:count`, `x402:last` carries the receipt URL).
24. **`X-CMR-X402: ready`** added to the CMR v1 identity set in `chrome.js` → every response of all 23 surfaces (verified on agentic-x402.ai and ca-cm-kg.ai). **Agent cards:** every node and product card (and allooloo.io, kyp-model.ai, roots) now carries `x-cmkg.x402 { endpoint, surface, price_usdc, receipts, jwks }` (verified uk-cm-kg.ai, agentic-radar.ai, allooloo.io, agentic-x402.ai). The Azure door cards (`agent.<cc>-cm-kg.ai`) are in the door image — flagged for the next image, not rebuilt for this.
25. **Machine kit of record** on agentic-x402.ai: llms.txt, facts.json, agent card, MCP server card, api-catalog, auth.md, AS metadata, robots, security.txt, sitemap (lists /api); markdown twin on `Accept: text/markdown`. Surfaces 2026-09-13.6; snapshots `2026-09-13-6` and `-7`.
26. **Test client** `RAILS\x402_pay.mjs` (x402-fetch + viem): the CEO runs it with the wallet key in an env var from their own file; prints status, PAYMENT-RESPONSE and the receipt URL, never the key. CC does not hold wallet keys and does not sign transfers, so the settled test is the CEO's leg; test USDC on Base Sepolia comes from the Circle faucet.
27. **CDP key on disk (CEO, ~15:35 UTC):** `AGENT KEYS\cdp-api-key-id.txt` (UUID) + `cdp-api-key-secret.txt` (Ed25519, base64 64 bytes) → Worker secrets `CDP_API_KEY_ID`, `CDP_API_KEY_SECRET`. The Worker's facilitator for Base mainnet switched itself to `https://api.cdp.coinbase.com/platform/v2/x402` (radar.json `paid_calls.facilitator`); a dummy payment on the trades route reached CDP with the per-request EdDSA JWT and was refused as an invalid payload (HTTP 400), not unauthorized (401) — the auth works. **FLAG:** CC's shape check printed the secret (minus its last character) into the session transcript — a leak under the standing rule; rotation requested in the report; the key id and every other key stayed unprinted. isitagentready rescan of agentic-x402.ai: Level 5, 15/5/2, `commerce.x402` PASS. Report `AGENTIC-X402-REPORT-2026-09-13.md`.
28. **CDP secret rotated (CEO, ~15:50 UTC):** new id + secret read from the same two files and re-uploaded as Worker secrets (only a sha256 prefix of the secret was shown, 0eafab03); a dummy payment on the trades route again reached CDP and was refused as invalid payload (400), not unauthorized — the rotated key is live. The leaked value is dead.
29. **Payer wallet (CEO, ~16:00 UTC):** fresh secp256k1 key generated locally with viem → `AGENT KEYS\x402-payer-key.txt` (git-ignored, never printed); address `0x510EF4d831fba1248D430d932FeD6c8e0BEbF339`. `RAILS\x402_pay.mjs` reads the key from that file (env `X402_PRIVATE_KEY` overrides); deps x402-fetch + viem installed in `CM-KG\RAILS` (package.json, node_modules ignored). One command: `node x402_pay.mjs https://agentic-x402.ai/api`. Balance at generation: 0 test USDC on Base Sepolia — fund from the Circle faucet before the call (EIP-3009 needs no ETH in the payer). Two `\x40` mangles in the comment/message fixed by Edit.
30. **Client to protocol v2 + settled test (CEO, ~16:15 UTC):** the CEO's run failed on the v1 package (`x402-fetch` 1.2.0 expects "base-sepolia", `/api` answers v2 CAIP-2 "eip155:84532"). Switched `RAILS\x402_pay.mjs` to the x402-foundation v2 packages `@x402/fetch` + `@x402/evm` 2.25.0 (`wrapFetchWithPaymentFromConfig`, `ExactEvmScheme`, network `eip155:*`); v1 package removed. Re-test run by CC on Base Sepolia (test USDC, no monetary value; mainnet stays the CEO's hand): **402 → signed EIP-3009 authorization → 200**. Settlement tx `0xe9b6efaf2d84b14fb164de92a7d659d0308e530edcfbb1187b937096196df60d` (Base Sepolia block 46771508, status 0x1, 2 logs; explorer https://sepolia.basescan.org/tx/0xe9b6efaf2d84b14fb164de92a7d659d0308e530edcfbb1187b937096196df60d), amount 10000 units = $0.01 USDC, payer `0x510E…F339`, facilitator x402.org. Receipt `https://agentic-x402.ai/x402/receipt/b38b7093612b101fdaa4a85ae6d6fc2d` resolves (settled true); its EdDSA JWT verifies against `/x402/jwks.json` (kid allooloo-x402-receipts-2026-09). RADAR paid-calls line: **1 settled**, last tx + receipt URL shown on the page and in radar.json. Commit `791fd52`.
31. **agentic-x402.ai /api → Base mainnet (CEO go, ~16:40 UTC):** Worker secret `X402_API_NETWORK=base`; page line 4.1 re-cut from "Network today: Base Sepolia (testnet first …)" to "Network: Base mainnet (eip155:8453, mainnet USDC; on the operator's word, Sept 13 2026 …)" — zero "Sepolia" mentions on the page; surfaces 2026-09-13.7. Verified by fetch: `/api` and `/api?record=…` answer 402 with scheme exact, network `eip155:8453`, asset mainnet USDC 0x8335…2913, amount 10000 ($0.01), 60 s window, pay-to unchanged (same 42-char address), `PAYMENT-REQUIRED` header; facilitator for Base = Coinbase CDP with the rotated key (radar.json `paid_calls.facilitator`). No test call — mainnet calls stay the CEO's hand. Two build slips on the way (a `→` in a print under cp1252 broke the chain; the `\'` in the JS string was lost by a regex replacement and esbuild refused — both fixed before deploy; the live Worker never served a broken build since the failed deploy did not upload).
32. **FOOTER — x402 (CEO, ~16:55 UTC):** `chrome.js` footer row on all 23 surfaces now `… · CEO Letter · x402 · Microsoft AI Cloud Partner · © 2026 …` (x402 → https://agentic-x402.ai/, same row, same style); allooloo.io's own line ends `… · CEO Letter · x402`. Surfaces 2026-09-13.8. Verified by fetch after propagation (~25 s; the first fetch still served .7): uk-cm-kg.ai, allooloo.io, agentic-x402.ai, kyp-model.ai all carry the link in place.

## LISTINGS — Allooloo doors (CEO order, Sept 13 2026, ~17:00–17:45 UTC)

33. **Registry:** card io.github.allooloo/cm-kg confirmed current (apex door, five tools, description of record; no "signed" on the live card) and republished as **0.12.0** with `x402` and `registries_gate` metadata blocks and a `confirmed` line (records confirmed against the public filing, not signed by it); mojibake em-dashes fixed; old card kept as `CM-KG\DOOR\server-0.11.0.json`. Registry JWT had expired → `mcp-publisher login github --token` with the allooloo GitHub token (no browser); publisher-provided metadata capped at 4 KB (4,296 → 3,915 bytes). **API Center** allooloo-apic: OpenAPI definition imported by link from each door's own spec for the eleven node doors + apex (exports read back the door titles), externalDocumentation openapi links corrected (were malformed), apex title corrected ("Canada" → "apex router"); new `agentic-x402-api` (rest, v1, OpenAPI from the new `https://agentic-x402.ai/openapi.json`, deployment prod → /api). `swagger-petstore` sample left in place, flagged. Rail `AZURE\apic_listings.py`. **Bazaar:** `extensions.bazaar` discovery block on both 402 payloads (per-route descriptions ≤500 chars — the first cut was 504 and trimmed), CDP validate returns the extension for both resources with no errors; indexing happens on the first settled CDP mainnet call = the CEO's hand; discovery list `https://api.cdp.coinbase.com/platform/v2/x402/discovery/resources`. Surfaces 2026-09-13.9; commit `e2d5892`. Report `LISTINGS-REPORT-2026-09-13.md`.
34. **x402 MAINNET fix (CEO note, ~18:00 UTC):** the CEO's first mainnet call on `/api` was refused by the facilitator as `invalid_payload`, no funds moved. Cause: the 402 offer's `extra.name` was "USDC" on every network; the Base mainnet USDC contract's EIP-712 domain is name "USD Coin", version "2" (the Base Sepolia test contract is named "USDC", which is why the testnet run passed). Fix: `NETWORKS[*].name` per network, `extra: { name: net.name, version: '2' }` — both paid routes. Surfaces 2026-09-13.10. Verified by fetch: `/api` and the trades record route answer 402 with network eip155:8453, mainnet USDC, `extra {name: "USD Coin", version: "2"}`. Awaiting the CEO's re-run; that first settled CDP call also triggers the Bazaar index.
35. **x402 MAINNET SETTLED (CEO call, ~18:10 UTC) — lane closed:** the CEO's re-run of `node x402_pay.mjs https://agentic-x402.ai/api` on Base mainnet: 200. Confirmed from KV: receipt `772c00ad2b43355529186825bff6201c` resolves, `settled: true`, network eip155:8453, amount 10000 = $0.01 USDC, payer `0x510E…F339`, facilitator Coinbase CDP; its EdDSA JWT verifies against `/x402/jwks.json`. On-chain (Base mainnet RPC): tx `0x925d3d63fdede5f00af2b2ef777586784586b994607a887a9874da92c9266d03`, block 51264818, status success, 2 logs, to the USDC contract 0x8335…2913. RADAR: **2 settled**, last tx and receipt URL on the page and in radar.json. **Bazaar:** `https://agentic-x402.ai/api` is indexed — CDP validate reports `index.active: true` (crawled 17:03 UTC) and the resource appears in the discovery list (15,284 resources scanned in full; listed with our description). The trades record route is not yet indexed (`index: null`): the Bazaar indexes per resource on its own first settled CDP call, and no mainnet call has been made on `agentic-trades.ai/x402/record/…` yet — one $0.01 call on that route (CEO's hand) lists it. The earlier `invalid_payload` attempt moved no funds. Receipt and tx recorded; nothing deleted.
36. **x402 trades record route settled on mainnet (CEO call, ~18:25 UTC):** confirmed from KV (`x402:last`): resource `https://agentic-trades.ai/x402/record/uk-cm-kg/LSE/BARC`, tx `0xcea55b25f437e292a8e44140457b5b8db60f423cb21aa403adbefd8de28c7f10`, network eip155:8453 (the trades route carries its receipt in PAYMENT-RESPONSE; the resolvable EdDSA receipt is the agentic-x402.ai endpoint's). On-chain (Base mainnet): block 51265147, status success, 2 logs, to the USDC contract. RADAR: **3 settled** (page + radar.json), last call = this tx. **Bazaar:** CDP validate reports `index.active: true` for both resources (`/api` crawled 17:03 UTC, trades route 17:14 UTC); the public discovery list (15,304 resources, scanned in full twice, filters ignored by the endpoint) shows `https://agentic-x402.ai/api` and not yet the trades route — the list lags the index. One later check, no polling: Task Scheduler "Allooloo x402 Bazaar listing check 2026-09-13" at 15:30 local = 21:30 UTC (`RAILS\bazaar_check.py` → `AZURE\bazaar-check-<stamp>.txt`). Lane closed: two paid routes on Base mainnet, three settlements ($0.01 test + two × $0.01 mainnet), receipts kept, RADAR counting, both resources indexed by CDP.
37. **API CENTER descriptions (CEO, ~18:45 UTC):** the description of record ("What: KYP Agentic Trading with the Capital Markets Knowledge Graph … No prices, no quotes, no licensed data.", 565 chars) on all twelve MCP assets; contact mk@allooloo.ai (Allooloo Technologies Corp., allooloo.io) on all thirteen; titles carry the node: "Capital Markets Agents: MCP+A2A Canada door" … "… apex router". API Center caps a title at 50 characters — "United Kingdom door" made 51 and was refused (ValidationError), so the UK asset carries "… UK door" (the only node abbreviated; every other node name fits). Every other property carried forward unchanged; read back after each PUT. Rail `AZURE\apic_descriptions.py` → `AZURE\apic-descriptions-2026-09-13.json`. **Flag:** the machine's az CLI default context had been switched to another tenant (a GreenCore login, user mk@gsc-em.com) since the listings run two hours earlier — the rail's tenant assert stopped before any write; context set back to the Allooloo subscription (tenant 04a24e43…, user mkeddy@…allooloo296) and confirmed with `az account show`, then the writes ran. The earlier listings writes had passed the same assert under the right tenant.
38. **STANDING RULE — Azure CLI, one root at a time (CEO, ~19:00 UTC):** logged in both roots: `C:\ALLOOLOO\START_ME_UP\AZURE-CLI-ONE-ROOT-RULE.md` and `C:\GREENCORE\START_ME_UP\AZURE-CLI-ONE-ROOT-RULE.md` (the only write CC makes under GREENCORE, as ordered; nothing read there). Allooloo guard `AZURE\az_guard.py` (`require_allooloo()`: tenant 04a24e43…, subscription 038b49c0…, user *@…allooloo296.onmicrosoft.com; prints the three on match, stops on any mismatch with the take-back command); `apic_listings.py` and `apic_descriptions.py` now call it in place of their inline tenant assert. CLI context at the time of logging: Allooloo (tenant 04a24e43…, subscription 038b49c0…, user mkeddy@…allooloo296).
39. **LISTING PACKS (CEO, ~19:20 UTC) — packs untouched.** (1) URL check: 16 distinct URLs across both packs; 15 answer 200 by GET; `https://mcp.capitalmarketsknowledgegraph.ai/mcp` answers 405 to a plain GET by design (Streamable HTTP door) and 200 to a JSON-RPC `initialize` POST (serverInfo returned) — the pack's "answered 200" line holds for the protocol the door speaks. (2) Wire aligned: estate-served A2A agent cards and the MCP server card `version` 0.11.0 → **0.12.0** (`aeo.js`, surfaces 2026-09-13.11) — apex card at capitalmarketsknowledgegraph.ai, every node card at `<cc>-cm-kg.ai`; apex Worker `cm-kg-apex` `SERVER_VERSION` → 0.12.0 (agent.capitalmarketsknowledgegraph.ai card and MCP serverInfo). Live by fetch: all 0.12.0. Not bumped: the Azure door images (`agent.<cc>-cm-kg.ai` cards, door serverInfo) still say 0.11.0 — rides the next image build with the `booted`→`refreshed` rename; flagged. Registry card 0.12.0: **its `description` is NOT the API Center What/Why text** — the registry caps `description` at 100 characters, so the 565-char text cannot be the description; 0.12.0 carries the 96-char CEO line ("Know every Canadian and UK issuer…"), and its `description_of_record` metadata is the older Sept 12 sentence. Registry versions are immutable: putting a KYP-first 100-char description and the What/Why text verbatim in the metadata means a **0.12.1** publish, which would put the wire one number ahead of the packs' "0.12.0" — held for the CEO's word rather than breaking practice [6]. "signed": none on the registry card (the only occurrence is "not signed by it"), none on any agent card or server card. (3) Assets in `C:\ALLOOLOO\SITE`: `avatar-400.png` 400×400 · `allooloo-logo.svg` (8,807 bytes) · `allooloo-logo-dark.svg` (8,856 bytes) · `icon-512.png` 512×512, live at capitalmarketsknowledgegraph.ai/icon-512.png as 512×512. Also present: icon-192.png 192×192, apple-touch-icon.png 180×180, allooloo-logo-1200.png 1200×300, allooloo_linkedin_logo_300.png 300×300. Snapshot 2026-09-13-10.
40. **REGISTRY CARD 0.12.1 (CEO go, ~19:45 UTC):** description "KYP Agentic Trading with the Capital Markets Knowledge Graph — eleven markets, in-country." (90 chars); `description_of_record` = the API Center What/Why text verbatim; `confirmed` line kept. Metadata 4 KB cap bit again (4,328 bytes): `estate` shortened and the twelve `agent_cards` URLs folded to one pattern line → 3,697 bytes. Publish path: the registry JWT had expired again, and `gh auth token` was handing out the greencore-solutions token (the active gh account had flipped since the 0.12.0 publish — same class of flip as the az context) → 403 "permission to publish io.github.greencore-solutions/*"; `gh auth switch --user allooloo`, re-login, published. Registry reads back: **0.12.1 active, isLatest true**, KYP-first description, What/Why in the metadata, no "signed" (only "not signed by it"); 0.12.0 stays as history. Wire: estate-served A2A cards (apex + every node + products), MCP server card, apex Worker `SERVER_VERSION` and MCP serverInfo all **0.12.1** by fetch (surfaces 2026-09-13.12; cm-kg-apex redeployed). Azure door image cards stay 0.11.0 until the queued build. Note: the agentic-x402.ai agent card carries the word "signed" in its title of record ("a paid call with a signed receipt") — the cryptographic sense, receipts are EdDSA-signed; records are not described as signed anywhere. Old cards kept: `server-0.11.0.json`, `server-0.12.0.json`. Snapshot 2026-09-13-11.
41. **Rule extended to the GitHub account (CEO, ~19:55 UTC):** `AZURE-CLI-ONE-ROOT-RULE.md` in both roots now carries item 5 — before any registry publish or git push, confirm the active gh account is the estate's own (allooloo / greencore-solutions) and switch if not, in the same command as the token read; the second incident (0.12.1 publish 403 under the flipped account) logged as the why. Allooloo guard `AZURE\az_guard.py` gained `require_github(account)` (reads `gh auth status`, switches if another account is active, stops if the estate account is not in the keyring); run bare it reports both the az context and the gh account — confirmed Allooloo on both at the time of logging. A `\a` (bell) mangle in one written path was found and fixed in both files.
