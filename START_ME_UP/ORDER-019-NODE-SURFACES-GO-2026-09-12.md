# 018 CLOSE-OUT + ORDER-019 GO (CEO, Sept 12 2026) — verbatim

1. Certificates: Cloudflare Origin CA on every regional door and
   Agent Card hostname — issued over the CF API, uploaded to the
   Container App, SSL full (strict), then flip orange. No managed
   certs, no re-bind, no polling. Report which are orange at the
   end of 019.
2. Registry: publish v0.11.0. Card description of record:
   "Know every Canadian and UK issuer, agent-ready. Instantly.
   Provably. Eleven markets, in-country."
3. Old Worker + D1, failed environment, failed executions: leave
   standing, listed for CEO deletion at end of scope.

4. ORDER-019 NODE SURFACES: GO. Boot file START_ME_UP\NODE-SURFACES.md
   as delivered — twelve surfaces, Formspree block, live fields,
   Numbers Log. Fly-by-wire. Sweeps stay disabled until CEO word.

5. AEO + AGENT JSON — standing on every surface in this order and
   every zone in item 6:
   - open to AI crawlers: robots.txt allows GPTBot, ClaudeBot,
     Google-Extended, PerplexityBot, and all others by default;
     Cloudflare AI Crawl Control / Block AI bots OFF, Bot Fight Mode
     OFF, managed robots.txt OFF, pay-per-crawl OFF
   - machine kit at the root of every zone: /llms.txt, /facts.json,
     /.well-known/agent-card.json, /.well-known/security.txt,
     /sitemap.xml, /robots.txt — and where a door exists, /mcp.json
     and /openapi.json
   - every page: self-canonical, <title> + meta description, WebSite
     JSON-LD with name "Allooloo", og:site_name, the favicon set
     (favicon.svg, .ico, 48/96/144/192/512, apple-touch-icon)
   - CMR headers on every response; strict CSP, HSTS; zero
     third-party scripts
   - counts in any kit file are live from list_nodes, never typed

6. NEW ZONES — 32, flick them live now so agents can see and use:
   agentic-trades · agentic-ask · agentic-coverage · agentic-esg ·
   agentic-issuers · agentic-registries · agentic-disclosure ·
   agentic-radar — each × .ai .com .org .io
   Per family: .ai is canonical and serves the quiet-zone kit from
   the estate Worker (root + the item-5 kit, one-paragraph facts.json
   naming the product and pointing at allooloo.io); .com .org .io →
   house wildcard 301 to the .ai. All 32: item-5 Cloudflare settings
   applied, orange, SSL full (strict). Exactly these 32; nothing
   else in the estate touched.
   Product page copy for the eight .ai roots comes later (ORDER-020);
   today they are live, open, kitted and forwarding.

REPORT (one, at 019 close): twelve node surfaces live; 32 zones
state (kit served, forwards, Cloudflare settings per zone); certs
orange per hostname; registry v0.11.0 published; pairwise
shared-text figure; Numbers Log seeded.
