# ORDER-019 — NODE SURFACES + 018 close-out — closing report (Sept 12 2026)

Fly-by-wire; shape calls in `ORDER-019-BUILD-LOG.md` (16 items). Nothing of ours deleted or overwritten; new output beside old. Sweeps stay disabled; `POND\.build-lock` stays.

## Twelve node surfaces — live
Every `<cc>-cm-kg.ai` root answers 200 with its nine-section page (head, 1 Scope … 9 Contact); `.org` and `.com` 301 to the `.ai` with path and query kept. Served by the estate Worker `estate-pages`, new entry `CM-KG\ESTATE\worker\surfaces.js` (previous `index.js` kept beside it).

| Surface | Section 2 example (real wire capture, 2026-09-12) | Counts on the page (live from list_nodes at close) |
|---|---|---|
| https://ca-cm-kg.ai/ | `TSX:SHOP` → Shopify Inc. | 4,820 records · 25,222 events · drop 2026-09-10 |
| https://us-cm-kg.ai/ | `NASDAQ:AAPL` → Apple Inc. | 7,710 · 1,872,343 · 2026-09-12 |
| https://uk-cm-kg.ai/ | `LSE:SHEL` → SHELL PLC | 1,570 · 128,393 · 2026-09-11 |
| https://fr-cm-kg.ai/ | `XPAR:FR0000120271` → TOTALENERGIES | 712 · 9,999 · 2026-09-11 |
| https://nl-cm-kg.ai/ | `AMS:NL0000009165` → HEINEKEN | 123 · 2,707 · 2026-09-11 |
| https://ch-cm-kg.ai/ | `NESN.SW` → NESTLE N | 829 · 1,385 · 2026-09-11 |
| https://de-cm-kg.ai/ | `SAP.DE` → SAP SE O.N. | 3,436 · 35,403 · 2026-09-11 |
| https://au-cm-kg.ai/ | `ASX:BHP` → BHP GROUP LIMITED | 1,874 · 124,300 · 2026-09-11 |
| https://sg-cm-kg.ai/ | `D05.SI` → DBS GROUP HOLDINGS LTD | 639 · 3,510 · 2026-09-11 |
| https://jp-cm-kg.ai/ | `7203.T` → TOYOTA MOTOR CORPORATION | 3,964 · 39,274 · 2026-09-12 |
| https://kr-cm-kg.ai/ | `005930.KS` → SAMSUNG ELECTRONICS CO,.LTD | 2,802 · 143,297 · 2026-09-12 |
| https://hk-cm-kg.ai/ | none — local-partner variant, no door | "not served (Width 0)" |

Live-field bindings confirmed: issuer count, event count and drop date are read from `list_nodes` on each render (five-minute cache); event counts were added to the apex node list from each store's facts for this (build log 6). The United States renders the full page (its door answers). Hong Kong renders the partner variant. Sections 4–6 carry each rail README's facts with the Gaps note verbatim and its source named; British spelling on uk; Japanese and Korean titles as published.

**Pairwise shared text (once, no rig):** 66 pairs, 8-word shingles over the smaller page of each pair, rendered text with tags stripped — max 39.5% (ch–de), mean 30.6%, 0 pairs at or above 40% (`AZURE\shared-text-019.txt`). First cut had 8 pairs over 40%; per-node residency, reading-order and sweep-clock clauses and a European re-cut brought every pair under.

**Form:** Formspree `moeqzgll` block on every node surface, the eight agentic roots, the estate roots and allooloo.io (its site form moved from `xbgjwaen`, fields kept, hidden `_subject`/`_gotcha`/`_next` added). Posting confirmed with one test submission each from the uk node form and the allooloo.io form.

**Numbers Log seeded** (`NODE-SURFACES.md` §9): twelve lines, node · drop date · records · events · seed.

## 32 zones — state (item 6)
All 32 bound to `estate-pages` as Workers custom domains (the house forward: custom domain + 301 in the Worker). Verified 32 of 32: the eight `.ai` roots answer 200 with the quiet-zone page and kit; every `.com`, `.org`, `.io` answers 301 to the `.ai` with path and query kept.

| Family | .ai (canonical, kit served) | .com / .org / .io | Bot settings · SSL |
|---|---|---|---|
| agentic-trades | 200, facts "Agentic Trades" | 301 → .ai (×3) | AI bots open · Bot Fight off · managed robots off · pay-per-crawl off · Full (strict) |
| agentic-ask | 200, facts "Agentic Ask" | 301 → .ai (×3) | same |
| agentic-coverage | 200 | 301 (×3) | same |
| agentic-esg | 200 | 301 (×3) | same |
| agentic-issuers | 200 | 301 (×3) | same |
| agentic-registries | 200 | 301 (×3) | same |
| agentic-disclosure | 200 | 301 (×3) | same |
| agentic-radar | 200 | 301 (×3) | same |

Kit on each `.ai` root: `/llms.txt`, one-paragraph `/facts.json` (product name, pointer to allooloo.io, live node counts), `/.well-known/agent-card.json` (no endpoint yet; points at the estate agent), `/.well-known/security.txt`, `/sitemap.xml`, `/robots.txt` (GPTBot, ClaudeBot, Google-Extended, PerplexityBot and 18 others allowed by name, all others by default), WebSite JSON-LD "Allooloo", og:site_name, canonical, favicon set (svg, ico, 48/96/144/192/512, apple-touch-icon), CMR headers, strict CSP, HSTS, zero third-party scripts. Product copy waits for ORDER-020. Zone settings were already as ordered on all 44 zones read (twelve node `.ai` + 32) — confirmed, not changed (`AZURE\zone-settings-019.txt`). `agentic-zen.*` (three zones, not in the 32) untouched.

## Certificates — orange per hostname (018 item 1)
Cloudflare Origin CA, one certificate per hostname (15-year), issued over the API with the Origin CA token the CEO put on disk, uploaded to each Container Apps environment, bound, then flipped orange. No managed certificates, no re-bind loops, no polling (rail `AZURE\origin_ca.py`, log `AZURE\origin-ca.log`).

**22 of 22 hostnames: binding SniEnabled · CNAME orange · zone SSL Full (strict):** mcp. and agent. for ca, uk, au, sg, ch, de, fr, nl, jp, kr, us. All eleven doors answer `/healthz` 200 and all eleven Agent Cards answer through Cloudflare (`AZURE\hostnames-019.txt`). The Failed managed-certificate objects from 018 stand untouched. Before the token landed the three API tokens on record were refused ("User is not authorized"); that was HITL line one and is closed.

## Registry (018 item 2)
`io.github.allooloo/cm-kg` **v0.11.0 published** and reads `isLatest: true` on the registry; description of record "Know every Canadian and UK issuer, agent-ready. Instantly. Provably. Eleven markets, in-country." (96 characters). Device code 02E6-3388 entered by the CEO; login and publish chained.

## Item 3 — standing, listed for CEO deletion at end of scope
Old `cm-kg-door` Worker + D1 `cm-kg`; failed environment `cmkg-env-eastus`; failed US job executions `cmkg-us-rail-innnyur`, `-fd5vrmb`, `-ulhdixz`, `-3gyuke4`; the 018 Failed managed certificates (23). None deleted.

## Also landed on the doors (item 5 on the machine surfaces)
Regional door image `cmkg-door:0.11.1` on all eleven apps and the apex router: `/robots.txt` allow list, `/.well-known/security.txt`, `/sitemap.xml`, `/mcp.json`, `/openapi.json`; strict CSP, HSTS, nosniff, no-referrer on every response. US Perplexity pages run (`cmkg-us-rail-9rq7q3z`, image 0.1.4) was in flight at close; it re-renders the East US door with decoded entity values when it finishes.

## HITL — needs MK
1. **Facts count vs record count** on two doors: Switzerland facts 829 against 626 record files; Korea 2,802 against 2,759 (43 KIND lines share a code). Surfaces print `list_nodes` as the boot requires; the fix is a records re-render under its own order.
2. **Sweeps + BUILD lock:** re-arm the weekly motion (§5 of the boot) and remove `POND\.build-lock` on the CEO's word; the Numbers Log starts moving then.
3. **ORDER-020:** product page copy for the eight `agentic-*.ai` roots (today: quiet-zone kit).
4. **Formspree `moeqzgll` settings** (CEO side): redirect → https://allooloo.io, notifications → developers@allooloo.ai (the boot names them; not verifiable from here).
5. **Hong Kong** local partner (standing).
6. **Deletion list** (item 3 above) at end of scope.
7. **Anthropic admin key** for the balance line (standing); **au storage key1** regeneration (standing).

## Spend line
- **Azure:** no new resources in this order (two certificates per environment uploaded; eleven app revisions on image 0.11.1). Cost figure still blank — the Cost Management API answered 429 all day; consumption rows carry no cost for the new groups yet.
- **Cloudflare:** 22 Origin CA certificates (no charge), 32 custom-domain bindings on the existing Worker (no charge), Worker deploys ×6.
- **Lab:** none on ORDER-019 (no engine reads; wire captures are door calls). ORDER-018 US rail: Gemini 984 calls, 310,950 prompt / 28,595 output tokens ≈ USD 0.16 at list; Perplexity pages run in flight, usage in `pond/width0/2026-09-12/perplexity.json` when it closes. Tavily plan 80,283 of 100,000 (unchanged). Anthropic balance: HITL (admin key).
