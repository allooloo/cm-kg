# ORDER-019 — build log of shape calls (fly by wire), Sept 12 2026 — with the 018 close-out items 1–3

1. **018 close-out, certificates (item 1):** Origin CA issuance needs the Origin CA token; the three API tokens on record answered "User is not authorized" on `POST /certificates`. HITL line one until the CEO put `AGENT KEYS\cloudflare-origin-ca.txt` on disk (Zone · SSL and Certificates · Edit). Rail `AZURE\origin_ca.py`: per hostname (one certificate per door and per Agent Card hostname, as ordered) — RSA key + CSR (openssl) → Origin CA certificate (15 years) → PFX → uploaded to the Container Apps environment as a named certificate (`origin-<mcp|agent>-<cc>-2026-09-12`) → hostname added where missing and bound to that certificate (`hostname bind --environment … --certificate …`; the first pass without `--environment` was refused) → CNAME turned proxied only where the binding read SniEnabled; zone SSL Full (strict). Private keys stay under `AZURE\certs\` (git-ignored). Result: 22 of 22 hostnames SniEnabled and proxied; all eleven doors answer `/healthz` 200 and all eleven Agent Cards answer through Cloudflare. The Failed managed-certificate objects from 018 stand untouched.
2. **018 close-out, registry (item 2):** `server.json` description set to the CEO's text (96 characters), device code 02E6-3388 entered by the CEO, chained `login github && publish` → **io.github.allooloo/cm-kg v0.11.0 published** (eleven doors, twelve Agent Cards, `nodes_live` eleven).
3. **018 close-out, item 3:** old `cm-kg-door` Worker + D1, `cmkg-env-eastus`, the failed US job executions — all standing, listed for CEO deletion at end of scope. Nothing deleted.
4. **Boot file:** `START_ME_UP\NODE-SURFACES.md` on disk beside the order file (272 lines), run as filed. Sweeps stay disabled; `POND\.build-lock` stays.
5. **Where the surfaces live:** the estate Worker `estate-pages` already held every node root on `.ai/.org/.com` (94 custom domains), so ORDER-019 is a new entry module `CM-KG\ESTATE\worker\surfaces.js` (wrangler `main`), beside the previous `index.js` (kept, plus a dated copy). `.ai` renders the nine-section node page; `.org` and `.com` 301 to the `.ai` with path and query kept (the .org "node registry" page of 018 is replaced by the forward, as the boot's rule 0.2 says). Per-node inputs in `nodes.js`; section 2 wire captures in `examples.js`, generated from `CM-KG\ESTATE\examples-2026-09-12.json` (one real issuer per node, captured from the apex: TSX:SHOP, NASDAQ:AAPL, LSE:SHEL, XPAR:FR0000120271, AMS:NL0000009165, NESN.SW, SAP.DE, ASX:BHP, D05.SI, 7203.T, 005930.KS; Hong Kong none).
6. **Live fields:** `list_nodes` carried records and drop date but no event count, so `build_index.py` now reads each store's `door/facts.json` and puts `events` (and `issuers_with_events`) on the apex node list; the apex was rebuilt and redeployed. The Worker fills issuer count, event count and drop date from `list_nodes` on every render (five-minute cache); nothing typed. Hong Kong renders "not served (Width 0)" — the roster count is not on the wire, so it is not printed.
7. **United States:** the door answers, so the US page is the full nine sections (the boot's "partial until the door answers" does not apply). The counts on it are the East US store's.
8. **Sections 4–6:** per-node facts from the rail READMEs; Gaps notes carried verbatim where the README has one (au, sg, ch, de, fr, nl), otherwise the README's gap lines and the ORDER-017 build notes, with the source named on the page ("carried from: …"). British spelling on uk. Japanese and Korean titles as published.
9. **Item 5 kit on the doors:** regional door image `cmkg-door:0.11.1` (all eleven apps moved to it) and the apex router now serve `/robots.txt` with the named-crawler allow list, `/.well-known/security.txt`, `/sitemap.xml`, `/mcp.json` and `/openapi.json`, with strict CSP, HSTS, nosniff and no-referrer on every response. Node surfaces serve the same kit; `/mcp.json` and `/openapi.json` on a surface are pointers to the node door's descriptors (the surface is not a door). Favicon set completed with 48/96/144 PNGs generated from the 512.
10. **Cloudflare settings (item 5):** all 44 zones (twelve node `.ai` zones + the 32 agentic zones) read `ai_bots_protection: disabled`, `crawler_protection: disabled`, `fight_mode: false`, `is_robots_txt_managed: false` and SSL Full (strict) before the order — read and confirmed, nothing to change (`AZURE\zone-settings-019.txt`). The node `.org`/`.com` zones and the three `agentic-zen` zones were not touched (out of the 32).
11. **32 zones (item 6):** every zone already active in the account. The house forward is the estate Worker itself (custom domain + 301 in the Worker, the same method the node `.com` hosts use), so all 32 hostnames were bound to `estate-pages` as Workers custom domains: the eight `.ai` roots serve the quiet-zone page, `/llms.txt`, one-paragraph `/facts.json` (product name, pointer to allooloo.io, live node counts), `/.well-known/agent-card.json` (no endpoint; points at the estate agent), `/.well-known/security.txt`, `/sitemap.xml`, `/robots.txt`; the 24 `.com/.org/.io` hosts 301 to the `.ai` with path and query kept. Verified 32 of 32.
12. **Contact form:** Formspree `moeqzgll` block (email, message, hidden `_subject` = "<hostname> contact", `_gotcha`, `_next` = https://allooloo.io) rendered by the Worker on every node surface, the agentic roots and the estate roots; the two mail addresses printed beside it. allooloo.io's own site form moved from `xbgjwaen` to `moeqzgll` with the same hidden fields (its name/firm/markets fields kept; email now required; honeypot renamed `_gotcha`); site rebuilt and redeployed. Posting confirmed with one test submission each from the uk node form and the allooloo.io form (Formspree answered ok).
13. **Pairwise shared text:** measured on the rendered pages (tags stripped, 8-word shingles shared over the smaller page of each pair, 66 pairs). First cut: 8 pairs at or above 40% (max 45%, fr–nl). Per-node residency (3.8), reading-order (4.10) and sweep-clock (1.10) clauses were added — market facts that differ by construction — and the four European reading-order texts and the Netherlands sources/mapping/tiers re-cut. Final: **max 39.5% (ch–de), mean 30.6%, 0 pairs at or above 40%** (`AZURE\shared-text-019.txt`). No rig kept; a one-off measure.
14. **Numbers Log seeded** in `NODE-SURFACES.md` §9 from `list_nodes`: twelve lines (node · drop date · records · events · seed).
15. **Count note for the CEO:** on two doors the facts count and the record count differ — Switzerland facts 829 (SIX 810, BX 19) against 626 record files and index keys; Korea 2,802 against 2,759 (the 43 KIND lines that share a code render to one file each). The surfaces print `list_nodes` (the facts count) as the boot requires; the gap is in the ORDER-015/017 render (rows without a distinct key were not written as records). Fix belongs to a records re-render, not to this order — HITL list.
16. **Not touched:** node `.org`/`.com` zone settings, `agentic-zen.*`, any other estate zone; the old estate Worker source; the 018 managed certificates; the BUILD lock; the sweeps.
