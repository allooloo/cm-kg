# Cloudflare agent-access audit — every Allooloo zone in the account (50 of 51; de-cm-kg.ai is not in the account)

Date: 2026-09-10. Token: AGENT KEYS\cloudflare.txt. Values read and written through /zones/{id}/bot_management and /zones/{id}/settings/security_level.

| Zone | Group | Bot Fight Mode before → after | Block AI bots before → after | AI Crawl Control: training before → after | AI Crawl Control: search before → after | AI Crawl Control: user before → after | AI Labyrinth / crawler protection before → after | Managed robots.txt before → after | Content bots protection before → after | Security level before → after | Could not touch |
|---|---|---|---|---|---|---|---|---|---|---|---|
| allooloo.ai | Company | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| allooloo.com | Company | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| allooloo.io | Company | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| allooloo.org | Company | off | **block → disabled** | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| au-cm-kg.ai | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| au-cm-kg.com | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| au-cm-kg.org | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| ca-cm-kg.ai | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| ca-cm-kg.com | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| ca-cm-kg.org | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| capitalmarketsknowledgegraph.ai | Root — CM-KG | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| capitalmarketsknowledgegraph.com | Root — CM-KG | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| capitalmarketsknowledgegraph.org | Root — CM-KG | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| ch-cm-kg.ai | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| ch-cm-kg.com | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| ch-cm-kg.org | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| cm-kg.ai | Root — CM-KG | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| cm-kg.com | Root — CM-KG | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| cm-kg.io | Root — CM-KG | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| cm-kg.org | Root — CM-KG | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| cm-record.ai | Root — CMR | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| cm-record.com | Root — CMR | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| cm-record.io | Root — CMR | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| cm-record.org | Root — CMR | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| de-cm-kg.com | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| de-cm-kg.org | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| fr-cm-kg.ai | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| fr-cm-kg.com | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| fr-cm-kg.org | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| hk-cm-kg.ai | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| hk-cm-kg.com | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| hk-cm-kg.org | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| jp-cm-kg.ai | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| jp-cm-kg.com | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| jp-cm-kg.org | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| kr-cm-kg.ai | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| kr-cm-kg.com | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| kr-cm-kg.org | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| nl-cm-kg.ai | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| nl-cm-kg.com | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| nl-cm-kg.org | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| sg-cm-kg.ai | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| sg-cm-kg.com | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| sg-cm-kg.org | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| uk-cm-kg.ai | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| uk-cm-kg.com | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| uk-cm-kg.org | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| us-cm-kg.ai | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| us-cm-kg.com | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |
| us-cm-kg.org | Node | off | disabled | disabled | disabled | disabled | disabled | off | disabled | medium | Super Bot Fight Mode (no such setting on the Free plan; verified-bots fields absent); pay-per-crawl (no zone API); WAF custom rules (token not authorised); browser integrity check left on |

## Summary

Zones audited: 50. Changed: 1 — allooloo.org: ai_bots_protection.
Bot Fight Mode was off everywhere. Block AI bots was on for allooloo.org only, now off. AI Crawl Control categories (training, search, user) were disabled everywhere. Managed robots.txt was off everywhere. AI Labyrinth / crawler protection was disabled everywhere. Security level is medium on every zone (no zone in Under Attack mode).
Not reachable with this token or plan: Super Bot Fight Mode does not exist on Free-plan zones (no sbfm_* fields returned, so nothing to set to verified-bots-allowed); pay-per-crawl has no zone-level API and was not touched; WAF custom rules (http_request_firewall_custom) answer "request is not authorized" so no challenge rule against GPTBot, ClaudeBot, Claude-User, PerplexityBot, Google-Extended, Bingbot, Applebot or Grok could be read or removed; user-agent and IP access rules refuse the token. Browser Integrity Check is on (default) on every zone and was left as found.